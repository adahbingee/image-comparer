let cfg = {
	zoomStep: Math.sqrt(2.0),
    files: [],
	currentFileIdx:    0,             // file index
	currentFrameIndex: 0,             // frame index for video
	imgSizeX:          0,             // image frame size width
	imgSizeY:          0,             // image frame size height
    offsetX:           0,             // image offset from origin
    offsetY:           0,             // image offset from origin
    transparent:     1.0,             // image transparent
}